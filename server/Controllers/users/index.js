const { GetUser } = require('./getUser');
const { GetUsers } = require('./getUsers');
const { GetMyUserData } = require('./getMyUserData');

const Controller = { 
  GetUser,
  GetUsers,
  GetMyUserData
};

module.exports = Controller;